apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: desintegrador-info
spec:
  hosts:
  - "*"
  gateways:
  - bookinfo-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        port:
          number: 80
        host: desintegrador
